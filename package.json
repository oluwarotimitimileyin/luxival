{
  "name": "luxival-site",
  "private": true,
  "scripts": {
    "build": "eleventy && node scripts/inject-env.js",
    "dev": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
