{
  "name": "luxival-site",
  "private": true,
  "scripts": {
    "build": "eleventy && node scripts/inject-env.js",
    "dev": "eleventy --serve",
    "clean": "rm -rf _site",
    "index": "node scripts/auto-index.js",
    "deploy-and-index": "git push origin main && node scripts/auto-index.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@playwright/test": "^1.60.0"
  }
}
